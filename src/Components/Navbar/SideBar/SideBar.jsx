import styles from "./SideBar.module.css";
import SideBarElement from "./SideBarElement/SideBarElement";

const SideBar = (props) => {
  // let sideBarListElement = props.sideBarData.map((list) => (
  //   <SideBarElement name={list.name} image={list.image} />
  // ));
  // return (
  //   <div>
  //     <h3>Friends</h3>
  //     <nav className={styles.sideBar_nav}>
  //       <ul >
  //         {sideBarListElement}
  //       </ul>
  //     </nav>
  //   </div>
  // );
};

export default SideBar;
